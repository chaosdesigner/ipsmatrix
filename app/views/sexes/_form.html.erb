<%= form_with(model: sex) do |form| %>
  <% if sex.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sex.errors.count, "error") %> prohibited this sex from being saved:</h2>

      <ul>
        <% sex.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description_en, style: "display: block" %>
    <%= form.text_field :description_en %>
  </div>

  <div>
    <%= form.label :description_cz, style: "display: block" %>
    <%= form.text_field :description_cz %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
